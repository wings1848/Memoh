# SQLite sqlc Output

This directory is reserved for sqlc-generated SQLite code.

The PostgreSQL generator remains configured in the root `sqlc.yaml` until the
SQLite schema and query set are implemented under `db/sqlite/migrations` and
`db/sqlite/queries`.
